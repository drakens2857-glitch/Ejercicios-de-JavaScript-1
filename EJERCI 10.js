// Acceder a un formulario por su ID
const miFormulario =

    document.getElementById('formulario-contacto');

// A través de la colección forms
const formularios = document.forms;
const primerFormulario =
    document.forms[0];
const formularioPorNombre =
    document.forms['formulario-contacto'];
// Usando querySelector
const formQuery =
    document.querySelector('form.formulario - registro');